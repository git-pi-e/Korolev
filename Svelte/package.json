{
  "name": "korolev_sveltekit",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "rm -rf build && svelte-kit build && touch build/.nojekyll",
    "deploy": "npm run build && npx gh-pages -d build -t true"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "1.0.0-next.0",
    "@sveltejs/adapter-static": "1.0.0-next.0",
    "@sveltejs/kit": "next",
    "gh-pages": "3.1.0",
    "node-sass": "6.0.0",
    "svelte": "3.38.2",
    "svelte-preprocess": "4.7.3"
  }
}