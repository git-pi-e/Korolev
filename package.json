{
  "name": "korolev",
  "version": "3.3.2",
  "author": "Plutoniumm",
  "owner": "SEDSCelestiaBPGC",
  "type": "module",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "node prep && rm -rf build && svelte-kit build --verbose",
    "build:server": "pnpm i --no-frozen-lockfile && node prep && rm -rf build && svelte-kit build --verbose",
    "node-sass": "node-sass static/css/global.scss static/css/global.css --output-style compressed"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "next",
    "@sveltejs/kit": "next",
    "node-sass": "^7.0.1",
    "svelte": "^3.44.0",
    "svelte-preprocess": "4.7.3"
  }
}